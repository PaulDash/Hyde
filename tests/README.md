# Code tests

## Pester tests

Use `.\tests\Invoke-HydeTests.ps1` to run the suite with the repository's Pester 5 configuration.

## BPA tests

Use `.\tests\Invoke-HydeAnalyzer.ps1` to run `PSScriptAnalyzer` across the PowerShell source files in `src`.
